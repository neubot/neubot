- Remove the length of the queue from the informations returned by
  speedtest because this is not that interesting for the user.
- Allow to start a test from /speedtest.html.
- Perform SSI only if MIME type is "text/html".
